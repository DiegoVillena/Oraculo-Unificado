# AGENTS.md — Instrucciones para ZCode (Oráculo Unificado)

Este archivo es la **memoria persistente** del proyecto. ZCode lo lee al iniciar cada sesión en esta carpeta, antes de cualquier prompt del usuario. Mantenlo actualizado cuando haya cambios estructurales.

---

## Qué es este proyecto

App Android híbrida (Capacitor + WebView) que unifica Tarot, I Ching y Carta Astral con análisis IA (Gemini). Sin framework JS — HTML5 + CSS3 + ES modules vanilla. Stack completo en `README.md`.

- **applicationId**: `com.oraculounificado.app`
- **MainActivity**: `com.oraculounificado.app.MainActivity`
- **Web root**: `www/` (servido por Capacitor; `js/` es el source que se sincroniza con `www/`)
- **Stack**: Capacitor 8, Swiss Ephemeris WASM, SQLite (sql.js), Gradle 8, Java 17, R8/ProGuard

## Estructura del proyecto

```
js/                          # Código fuente (ES modules)
  core/                      # astrologia.js, analysis.js, ia-api.js
  data/                      # tarot-kb, iching-kb, sqlite-db, swisseph WASM, ciudades.sqlite
  i18n/locales/               # 6 idiomas: es, en, pt, fr, it, de
  ui/                        # tarot.js, astral.js, modal.js, tabs.js, onboarding.js, donacion.js, glossary.js
  main.js                    # Entry point
www/                         # Copia de js/ servida en el WebView (sincronizar tras editar js/)
android/                     # Proyecto nativo (Capacitor)
  app/src/main/java/.../MainActivity.java   # Puentes JS↔Nativo
  app/build.gradle           # Config build + firma release
worker/                      # Cloudflare Worker (proxy IA Gemini)
img/tarot/                   # Imágenes de las cartas
```

## Términos técnicos seleccionables (glosario beginner-friendly)

Cada término técnico (cartas de Tarot, hexagramas I Ching, planetas, signos, casas, aspectos) es **tapable**: muestra una descripción breve y clara. Sistema en `js/ui/glossary.js`.

- **`data-term="tipo:clave"`** — atributo en cualquier elemento HTML que lo hace seleccionable. `tipo` ∈ `tarot`, `iching`, `planeta`, `signo`, `casa`, `aspecto`. `clave` es **canónica**: nombre español de carta (`tarot:El Loco`), número de hexagrama (`iching:7`), nombre inglés de planeta (`planeta:Sol`), índice 0-11 de signo (`signo:0`), número 1-12 de casa (`casa:5`), tipo inglés de aspecto (`aspecto:Conjunction`). El texto visible es la traducción al idioma actual; `data-term` desacopla identidad de presentación.
- **Event delegation global**: `inicializarGlosario()` (llamada en `main.js`) instala un listener de `click` en `document` que detecta taps en cualquier `[data-term]`, incluso en HTML generado dinámicamente (análisis IA incluido). No hace falta reasignar handlers.
- **Cartas e I Ching** → abren el **modal existente** (`window.__tarotUI.abrirModal` / `abrirModalIching`). **Términos astrológicos** → abren un **popover flotante** (`.glossary-popover`, CSS en `styles.css`) con título + descripción breve.
- **`envolverTerminos(html)`** — función exportada de `glossary.js` que post-procesa HTML y envuelve ocurrencias de nombres de planetas/signos/cartas/aspectos en `<span class="term" data-term="...">`. Usada por `astrologia-analisis.js` (narrativa) y `ia-api.js` (análisis IA Tarot/Astral/Combinado). El análisis local de Tarot (`analysis.js`) envuelve manualmente los spans `.ref-tarot`/`.ref-iching` con `data-term`. La tabla y aspectos de Carta Astral (`astral.js`) envuelven con helper `_termSpan()`.
- **Glosario de descripciones**: bloque `glosario` en cada `datos-maestros-{locale}.json` con `planetas`, `signos`, `casas`, `aspectos` (1-2 frases por término). Helper `tGlosario(tipo, clave)` en `i18n.js`.
- **Reset al cambiar idioma**: `resetMapaTerminos()` se llama tras `cambiarIdioma()` porque los nombres traducidos cambian.
- **Señalización visual**: clase `.term` y selector `[data-term]` dan subrayado punteado discreto. El onboarding tiene un 5º paso (`onboarding.paso5_*`) que lo explica.
- **Cierre del popover**: tap fuera, botón atrás Android (vía `history.pushState`), scroll, resize, o tap en el mismo término (toggle).

## Sinastria (compatibilidad entre 2 cartas astrales)

Tab propio (`data-tab="sinastria"`) con su panel. Permite seleccionar 2 cartas astrales guardadas (de `storage.obtenerCartas()`) y calcular la compatibilidad.

- **Motor** (`js/core/astrologia.js`): `calcularSinastria(cartaA, cartaB)` → objeto `sinastriaLocalOutput` con 6 secciones (radarScores 5 dimensiones 0-100, globalScore + label, puntosFuerteTop/desafioTop, casasDestacadas, aspectosTop, promptDataText). Algoritmo de pesos: orbe 0-1°=100%, 1-3°=75%, 3-5°=50%, >5°=20%; armónicos (Trine/Sextile/Conjunción benéfica) suman, tensión (Square/Opposition/Conjunción maléfica) restan. 5 dimensiones: química (Venus-Marte/Casa8), mental (Mercurio/Casa3), emocional (Luna/Casa4), espiritual (Nodos/Neptuno/Casa12), estabilidad (Saturno/Casa7). `calcularAspectosSinastria(puntosA, puntosB)` calcula aspectos cruzados A×B. `casaDelPlanetaExport` para overlays. `generarRuedaSinastriaSVG(dA, dB, aspectosCruzados)` genera rueda bicarta (anillo interior A dorado `--sinastria-a`, anillo exterior B morado `--sinastria-b`, líneas de aspectos cruzados en el centro con opacity según orbe).
- **UI** (`js/ui/sinastria.js`): `initFormularioSinastria()` (2 selects + btn calcular), `render(resultado, cartaA, cartaB)` (rueda bicarta + radar pentagonal SVG + círculo central globalScore + pills fuerte/desafío + tarjetas casas + tabla aspectos colapsable). Animación de fusión 1.5s (`@keyframes sinastriaFusionA/B/Spark` en `styles.css`): 2 ruedas (A arriba dorada, B abajo morada) se atraen y funden. Reutiliza `mostrarAnimacionConstelaciones`/`ocultarAnimacionConstelaciones` (exportadas de `astral.js`).
- **Análisis IA**: `analisisSinastriaIA(textoA, textoB, scoresText)` en `ia-api.js` → POST `{tipo:'sinastria', datos:{carta1, carta2, scores}, idioma}`. Worker (`worker/oraculo-worker.js`): `PROMPT_SINASTRIA` (rol astrólogo + 4 secciones 🌟🏠⚡🔮). Fallback local si la IA falla (badge `.local-origen`).
- **Persistencia** (`js/storage.js`): key `sinastrias_guardadas` con `guardarSinastria/obtenerSinastrias/borrarSinastria/borrarTodasSinastrias`.
- **Wiring** (`js/main.js`): `window.__app.analizarSinastria/guardarSinastria/verSinastriasGuardadas/verSinastriaGuardada/borrarSinastria/borrarTodasSinastrias`. `window.__sinastriaUI` (copiar/compartir/mostrarInfoPlaneta/mostrarInfoAspecto).
- **i18n**: sección `sinastria` (~45 claves) + `tabs.sinastria` + `main.borrarSinastrias` en los 6 `{locale}.json`. Onboarding paso 6 (`onboarding.paso6_*`), `ONBOARDING_KEY` bumpeado a `_v2`.
- **Colores armónicos**: `--sinastria-a: #e8c46a` (dorado, persona A), `--sinastria-b: #b89cff` (morado armónico, persona B).

## Puentes JS ↔ Nativo (MainActivity.java)

ZCode debe mantener estos puentes al añadir features nativas. R8/ProGuard los conserva (ver `proguard-rules.pro`).

- **`AndroidClipboard.copy(text)` / `.read()`** — portapapeles nativo (navigator.clipboard no funciona en WebView sin HTTPS).
- **`AndroidShare.share(text)`** — menú compartir de Android. Usa `runOnUiThread` (las @JavascriptInterface corren en hilo de fondo), `ClipData` + `EXTRA_TEXT`. Debounce en JS evita choosers apilados. **Comportamiento por app**: Gmail/Telegram/Messages leen `EXTRA_TEXT` completo; WhatsApp impone su propio límite interno y trunca el texto a medias (no hay forma de forzarlo). Para textos >500 chars: genera un **PDF** con `android.graphics.pdf.PdfDocument` (API nativa, sin librerías), lo adjunta con `EXTRA_STREAM` vía `FileProvider`, MIME `application/pdf`, y **NO** incluye `EXTRA_TEXT` (si lo incluye, WhatsApp prioriza el texto truncado e ignora el archivo). El texto completo va en el PDF sin truncar, multi-página con wrapping automático. Textos cortos usan `EXTRA_TEXT` + `text/plain` normal.

## Flujo de trabajo (OBLIGATORIO)

### 1. Antes de tocar código
- Trabaja siempre en una **rama nueva** desde `main`: `git checkout -b <tipo>/<descripcion>` (tipos: `fix`, `feat`, `refactor`, `docs`).
- NUNCA trabajes directamente en `main`.

### 2. Reglas de edición
- **`js/` es el source.** Tras editar archivos en `js/`, sincroniza a `www/` y a `android/app/src/main/assets/public/` (es lo que ve el WebView). Capacitor no hace esto solo durante el build debug/release a menos que se ejecute `cap sync`; para cambios rápidos, copia manualmente los archivos modificados de `js/` → `www/` → `android/app/src/main/assets/public/`.
- Mantén el estilo del código existente (sin framework, ES modules, `t()` para i18n).

### 3. Verificación
- Compila y prueba antes de declarar terminado: `cd android && ./gradlew assembleDebug` (o `assembleRelease` si el usuario lo pide).
- Si el emulador está disponible, instala y prueba. Si no, deja claro al usuario que debe probar en su móvil.

### 4. Commit y push — CON CONFIRMACIÓN DEL USUARIO
- **NUNCA hagas commit ni push sin preguntar al usuario primero.** El usuario quiere testear personalmente antes de que nada entre al historial.
- Cuando la tarea esté lista para commit, propón al usuario:
  1. El mensaje del commit (formato: `<tipo>: <descripción breve>` — ej. `fix: truncamiento al compartir textos largos`)
  2. Si quiere push a la rama
- Espera su confirmación explícita antes de ejecutar `git commit` o `git push`.

### 5. Actualización de documentación
- **Si la tarea añade/cambia algo estructural** (nuevo puente nativo, nueva feature, cambio de build, nueva dependencia): actualiza este `AGENTS.md` y/o `README.md` como parte del commit.
- **Si es un bugfix aislado o cambio menor**: el mensaje del commit basta, no hace falta tocar la docs.
- El historial de commits (`git log --oneline`) es la documentación de qué se ha hecho entre sesiones — usa mensajes descriptivos.

## Comandos útiles

```bash
# Build debug
cd android && ./gradlew assembleDebug

# Build release firmado (APK en android/app/build/outputs/apk/release/)
cd android && ./gradlew assembleRelease

# Sincronizar js/ → www/ → assets (después de editar js/)
# Manual, archivo por archivo:
cp js/<archivo> www/js/<archivo>
cp js/<archivo> android/app/src/main/assets/public/js/<archivo>

# Ver historial de commits (contexto de tareas previas)
git log --oneline -20

# Instalar APK en dispositivo/emulador
adb install -r android/app/build/outputs/apk/release/app-release.apk
adb shell am start -n com.oraculounificado.app/.MainActivity
```

## Lo que NO se debe hacer

- No hacer commit en `main` directamente — siempre rama nueva.
- No subir `*.keystore`, `keystore.properties`, `.env`, `node_modules/`, `build/` (ya están en `.gitignore`).
- No usar `navigator.share()` directamente en JS — no funciona en WebView. Usar el puente `AndroidShare.share()`.
- No usar `navigator.clipboard` directamente — usar `AndroidClipboard.copy()`.
- No editar solo `www/` sin sincronizar `js/` (source of truth) — el orden correcto es editar `js/` y copiar a `www/`.

## Estado actual (última actualización: 2026-08-05)

- v1.0 subido a GitHub (commit inicial)
- Botones de copiar y compartir funcionando en Tarot y Carta Astral
- Fix de crash por choosers apilados (runOnUiThread + debounce + FLAG_ACTIVITY_NEW_TASK)
- Fix de truncamiento de textos largos al compartir (ClipData + FileProvider con .txt temporal, luego PDF para WhatsApp)
- APK release firmado y verificado en emulador
- **Términos técnicos seleccionables (glosario beginner-friendly)**: cualquier término (cartas, planetas, signos, casas, aspectos, hexagramas, conceptos) es tapable. Cartas/hexagramas abren el modal existente; términos astrológicos abren un popover flotante. Glosario breve en los 6 idiomas. Onboarding ampliado a 5 pasos. (rama `feat/terminos-seleccionables`)
- **Cartas invertidas**: al pinchar una carta invertida en el análisis (local o IA), abre el modal en modo invertido. `data-reves="1"` en spans. Post-procesador `envolverTerminos` detecta "Reversed"/"Invertida"/"(Inv)" etc. en el texto de la IA.
- **Imágenes .webp**: las cartas usan `.webp` (no `.jpg`). `getImgUrl` y `onerror` fallbacks actualizados.
- **Animación tarot**: barajado reducido a 1.5s (antes 2.5s).
- **Link Ko-fi**: puente nativo `AndroidOpenUrl.open(url)` abre URLs externas en el navegador del sistema. Modal "Acerca de" usa clase `visible`. `abrirKoFi` sin guión.
- **Detección de idioma**: puente nativo `AndroidLocale.get()` devuelve `Locale.getDefault().getLanguage()` (navigator.language en WebView no es fiable).
- **Sinastria (rama `feat/sinastria`)**: nuevo tab 💞 que calcula la compatibilidad entre 2 cartas astrales guardadas. Motor local con algoritmo de pesos (orbe + tipo de aspecto) que produce 5 puntuaciones de radar (química, mental, emocional, espiritual, estabilidad) + global. UI con rueda bicarta (A dorada interior / B morada exterior + aspectos cruzados), radar pentagonal SVG, círculo de score global, pills fuerte/desafío, tarjetas de casas y tabla de aspectos. Animación de fusión 1.5s (2 ruedas se atraen y funden). Análisis IA (prompt de astrólogo experto en sinastría, 4 secciones 🌟🏠⚡🔮) con fallback local. Persistencia de sinastrias guardadas. Onboarding paso 6. i18n en los 6 idiomas.
  - **Pruebas en emulador (2026-08-05)**: APK debug compilado e instalado en OraculoTest. Fix de bug `return` huérfano en `main.js` (función `renderTiradasGuardadas` perdió su declaración al añadir sinastria → abortaba `init()` antes de `initTabs()`). Tras fix, verificado: onboarding 6 pasos, tab Sinastria abre panel con 2 selects, cartas Diego (15/06/1990 Madrid) y Laura (20/12/1989 Barcelona) guardadas, sinastria calculada correctamente (74% "Afinidad Sólida y Constructiva", radar 61/72/92/73/66, rueda bicarta, pills fuerte/desafío, 6 tarjetas de casas, tabla de aspectos con 8 filas, botones copiar/compartir/guardar visibles). **No verificado**: botón "Analizar sinastria" (IA/fallback) — inaccesible por scroll del WebView en emulador. **Pendiente**: commit + push (esperando confirmación de Diego).
  - **Sistema jerarquizado + carta compuesta (2026-08-11)**: motor reestructurado a **8 factores con pesos que suman 100%** (Química 20%, Emocional 20%, Mental 15%, Espiritual 10%, Estabilidad 15%, Elementos 5%, Casa 7/Descendente 10%, Carta compuesta 5%). `calcularCartaCompuesta(cartaA,cartaB)` calcula el punto medio de cada par de planetas (arco más corto, `_puntoMedio`) + ASC/MC compuestos, casas Equal desde el ASC compuesto. Tabla `AMISTAD_PLANETARIA` (Graha Maitri, Brihat Parashara) aplicada como modificador ±3 en el scoring de aspectos. Factor casa 7/Descendente detecta planetas cerca del Descendente (orbe 8°) + regente de casa 7. Factor elementos promedia compatibilidad elemental. Factor compuesta valora armonía de Sol/Luna/Venus/Marte compuestos. El % global = suma ponderada de los 8 factores. UI: sección "Desglose de la puntuación" (8 filas con peso, score, barra y contribución) en `sinastria.js`/`index.html`/`styles.css`. Análisis local ampliado (desglose, carta compuesta, casa 7, amistad planetaria). `PROMPT_SINASTRIA` reajustado (5 secciones 🌟🪐🏠⚡🔮). Version tags bumpeados a `?v=19`.
  - **Verificación emulador (2026-08-11)**: APK debug reconstruido e instalado. Diego×Laura → **69% "Atracción con Matices"** (antes 74%). Desglose verificado: Pasión 65, Emocional 92, Mental 73, Espiritual 61, Estabilidad 67, Elementos 62, Casa7 50, Compuesta 45 → global 69% = suma ponderada exacta. Carta compuesta verificada manualmente (Sol 26°21' Piscis, Luna 24°41' Sag, Venus 26°51' Piscis, Marte 6°18' Acuario, ASC 260.16°, MC 174.15° — todos = punto medio correcto). Graha Maitri verificado contra tabla estándar (18 checks PASS). Análisis local verificado (todas las secciones nuevas presentes). **Pendiente**: (1) redesplegar `worker/oraculo-worker.js` a Cloudflare — el worker desplegado es una versión antigua SIN la rama sinastria (devuelve "Tipo no válido"), por lo que el análisis IA de sinastria siempre cae al fallback local hasta redesplegar; (2) commit + push (esperando confirmación de Diego).
  - **Sensibilidad 2.3 + amistad simétrica + neto + consejo (2026-08-22/23)**: multiplicador del radar ajustado a 2.3 (antes 2). `_grahaMaitri` hecho **mutuo/simétrico** (el resultado ya no depende del orden A/B — verificado: Diego×Ainoa idéntico en ambas direcciones). Añadido **neto de aspectos** (armónicos/tensos/neto, estilo CafeAstrology) con disclaimer breve, y **consejo accionable** del área más débil ("Punto a trabajar"). Detalle por factor en el desglose (qué aspectos contribuyen, con signo y persona). Planetas/ángulos con signo y persona en pills, tabla y casas. Glosario ampliado (medio cielo, ascendente, cuadratura, sextil, casas). Tabla de aspectos contraída por defecto. **Fix bug índices obsoletos**: los selects de sinastría se repueblan al abrir la pestaña (`pestana-cambiada` en tabs.js) y al guardar/borrar cartas (`poblarSelects` exportado y llamado en main.js). Version tags ~`?v=31`.
  - **Worker IA desplegado con sinastría (2026-08-23)**: redesplegado `worker/oraculo-worker.js` en Cloudflare desde el dashboard (Quick Edit). El análisis IA de sinastría ya funciona (antes caía al fallback local por "Tipo no válido"). Verificado en app (insignia "IA oracular"). El secret `GEMINI_KEY` se conservó. **Pendiente**: commit + push (esperando confirmación de Diego).
  - **REWORK: facilidades/desafíos por sector (2026-08-25)**: motor reestructurado a **8 factores nuevos** (Química 20, Emocional 20, Mental 15, Espiritual 10, Estabilidad 10, **Valores/Estilo de amor (Venus)** 10, **Transformación/Poder (Plutón)** 5, **Compromiso Casa 7** 10 → suma 100%). **Corrección de la tensión por sector**: en Química (Venus-Mars/Mars-Mars/Mars-Plutón) TODO contacto SUMA atracción por intensidad/orbe; cuadratura/oposición añade "fricción" (descriptor) sin puntuar negativo. En Transformación el contacto tenso se valora "intenso" (no malo). En el resto de sectores la tensión sigue bajando (es "requiere trabajo") con descriptor cualitativo. Nuevo scoring por sector en `PARES_FACTOR` (`astrologia.js`), con `_nivelFactor` → nivel **Facilidad/Matiz/Intenso/Desafío** por factor. Carta compuesta mejorada (lee más planetas + ASC/MC). UI: se sustituye el pentágono por un **PANEL de 8 barras horizontales** (nombre, score, color verde/amarillo/ámbar/rojo, nivel y descriptor) con el % global como resumen secundario. Fallback local (`main.js` + `sinastria-dictionary.js` con `fraseFactor`) y `PROMPT_SINASTRIA` reescritos para explicar facilidades/desafíos por sector y los nuevos factores. i18n nuevas claves (nivel_*, factorValores/Transformacion/Compromiso, factorDesc_*, panelNota, conFriccion, sector, leyendaSector, sectores, consejo_*) en los 6 idiomas. Version tags `?v=36`. Verificado en emulador: DiegoVO×Ainoa → **59% "Atracción con Matices"** (Pasión 67 Intenso-fricción, Emocional 62, Mental 54, Espiritual 71 Facilidad, Estabilidad 56, Valores 47 A trabajar, Transformación 61 Intenso, Compromiso 50), suma ponderada exacta, consejo apunta a Valores (el más débil), neto +3703/−1490, 6 casas, 8 aspectos, IA oracular responde. **Pendiente**: (1) redesplegar `worker/oraculo-worker.js` a Cloudflare (el desplegado aún usa el PROMPT_SINASTRIA viejo); (2) commit + push (esperando confirmación de Diego).
  - **Radar octogonal + fix alineación profesional (2026-08-25)**: se añadió un **radar octogonal (8 ejes)** como complemento visual del panel de barras (`generarRadarOctogonalSVG` en `sinastria.js`): cada vértice a la altura de su puntuación, coloreado por nivel (verde/amarillo/ámbar/rojo), % global en el centro, vértices tappables (`mostrarInfoFactor`). **Fix de alineación profesional**: los pares Venus-Plutón y Marte-Plutón (los aspectos de transformación/poder más importantes en sinastría) tenían `atraccion:true` + `sec:'transformacion'`, pero el `if (par.atraccion)` los capturaba primero y NUNCA alimentaban el factor Transformación. Corregido: ahora alimentan Química Y Transformación (tenso = intenso). Verificado: el detalle de Transformación de DiegoVO×Ainoa ahora incluye Venus (Leo) □ Plutón (Escorpio). Version tags `?v=37`. **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Ponderación por orbe + planeta personal (2026-08-25)**: alineado con la práctica profesional (Cafe Astrology, AstroStyle, Robert Hand, Stephen Arroyo, Liz Greene). Se sustituyó el **promedio aritmético** por una **media ponderada** (`_pesoOrb` + `_pesoPlanetaPar`, peso del aspecto = orbe exacto × planeta más importante). Un aspecto definitorio (orbe estrecho + planeta personal) ahora domina el sector; muchos aspectos amplios ya no lo diluyen. Peso recalibrado: **Estabilidad 12, Compromiso 8** (antes 10/10) para no sobreponderar la "viabilidad de pareja" (suma total sigue 100%). `_nivelFactor` intacto. Etiquetas del radar acortadas ("Transformación", "Valores", "Compromiso") y leyenda con puntos de color bajo el radar (sustituye a la frase explicativa). i18n: 4 claves nuevas `leyendaFluye/Matices/Intenso/Trabajar`. Version tags `?v=39`. Verificado: DiegoVO×Ainoa → **60%** (Pasión 74, antes 67), DiegoVO×Paula → **64%** (Pasión 89, antes 79) — la química exacta ahora sube como en lectura profesional. **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **UI simplificada + desglose técnico por sector (2026-08-25)**: se eliminaron del resultado las tarjetas "Punto fuerte"/"Desafío", el desplegable "Cómo leer esta sinastría" y las "Casas de impacto" (información técnica que el botón "Analizar sinastría" explica mejor). El **consejo "Punto a trabajar"** ahora se muestra **dentro del panel, justo debajo del radar** y se **adapta al número de sectores en rojo** (`_renderConsejo`: si hay varios, los lista con su consejo; plural vía `consejoTituloPlural`). El desplegable "aspectos clave" se convirtió en **"Detalles técnicos"** (`renderDetallesTecnicos`): tabla desplegable por los 8 sectores con peso, puntuación, aporte al % global y los aspectos que alimentan cada sector, más un **apartado "Datos completos"** con TODA la sinastría técnica: todos los aspectos cruzados (75 en Diego×Paula, ordenados por orbe), los planetas de cada carta con signo/grado/casa, la carta compuesta, las casas de impacto y la amistad planetaria (Graha Maitri). `getTextoCopia` ahora incluye los "Puntos a trabajar" (sectores en rojo). i18n: claves nuevas `detallesTitulo`, `consejoTituloPlural`, `todosAspectos`, `detallesCarta`, `posicion`. Version tags `?v=42`. Verificado: DiegoVO×Paula muestra "Puntos a trabajar: Estabilidad, Mental", pills/leyenda/casas eliminadas, tabla técnica por sectores + "Datos completos" con 75 aspectos. **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Prompt IA optimizado + copiado completo (2026-08-25)**: el botón "Analizar sinastría" ya NO envía la carta astral completa de cada persona (era pesada y desviaba el foco de la pareja); ahora envía un **bloque compacto por persona** (`_datosPersonaIA` en `main.js`: Sol/Luna/Mercurio/Venus/Marte/Júpiter/Saturno con signo, grado y casa + ASC/MC) más el `promptDataText` (sectores, niveles, aspectos, compuesta). El texto copiado (`getTextoCopia`) ahora incluye el **detalle de cada barra** (planetas + signos + tipo de aspecto: cuadratura/trígono/oposición), los **planetas de cada carta** (posición y casa) y la **carta compuesta**. El **fallback local** añadió la sección "Punto a trabajar" (sectores en rojo con su consejo). `_detalleFactor` del motor ahora incluye el nombre del aspecto (`tAspecto`). Sustituidas las etiquetas "A/B" por los nombres reales de las cartas en la tabla de aspectos y en las casas. Version tags `?v=49`. Verificado: el body enviado al worker trae `personaA = "DiegoVO\n- Sol: 16°24' Cáncer (Casa 6)..."` (compacto), IA responde 3018 caracteres, y el fallback local muestra "Puntos a trabajar: Estabilidad...". **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Refinamiento prompt/detalle (2026-08-25)**: `_generarPromptData` ya NO incluye "ASPECTOS EXACTOS Y CASAS CLAVE" ni "EJE CASA 7 / DESCENDENTE" (el compromiso queda en su sector). Las **PUNTUACIONES POR SECTOR** ahora incluyen el **detalle por sector** (planetas + signo + tipo de aspecto + **persona a la que pertenece cada planeta** + **grado del orbe**). `_detalleFactor` ahora añade "de {nombre}" y "orbe {g}" — esto afecta también al desglose de barras de la app y al texto copiado. Las "CASAS DESTACADAS" usan los nombres reales (no "Persona A/B") y la carta compuesta se etiqueta con la pareja ("la relación de X × Y"). Version tags `?v=50`. Verificado: el body del prompt trae el detalle por sector con nombres y orbes, sin los bloques eliminados; copiado y desglose de la app con "de DiegoVO ... (orbe 9°27', +8)". **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Copiado = bloque de datos de la IA (2026-08-25)**: `getTextoCopia` ahora copia **exactamente el mismo bloque de datos que se envía a la IA** en "Analizar sinastría" (`DATOS DE LAS PERSONAS` + `PUNTUACIONES Y ASPECTOS`), vía helper `_personaDataIA` (compacto por persona). Se eliminó el append del análisis en `copiar()`/`compartir()`. Version tags `?v=51`. Verificado: el copiado empieza con "DATOS DE LAS PERSONAS: ... PUNTUACIONES Y ASPECTOS: ..." (4057 chars). **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Botón "Copiar sinastria + análisis" (2026-08-25)**: tras generar el análisis (IA o local) aparece un botón `#btn-copiar-sinastria-analisis` (`_appendCopiarAnalisisBtn` en `main.js`) que copia **el bloque de datos + el texto del análisis** (`copiarSinastriaConAnalisis`, con feedback "✓ Copiado"). i18n: clave `copiarAnalisis` en 6 idiomas. Version tags `?v=52`. Verificado: el botón aparece tras el análisis y copia "DATOS DE LAS PERSONAS ... === ANÁLISIS DE SINASTRIA === 🌟 Alquimia...". **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Compartir incluye análisis (2026-08-25)**: `compartir()` ahora añade el análisis (IA o local) si se ha realizado (`window.__sinastriaUI._ultimoAnalisis`), quedando "datos + === ANÁLISIS DE SINASTRIA === ...". El fallback de compartir copia al portapapeles el texto completo. Version tags `?v=53`. Verificado: compartir manda "DATOS DE LAS PERSONAS ... ANÁLISIS DE SINASTRIA ..." (6754 chars). **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Ángulos + aspectos profesionales (2026-08-25)**: alineado con investigación de Cafe Astrology y escuelas relacionales (WebFetch directo funciona; el subagente con WebSearch se atascaba por webs bloqueadas). **Orbe estrecho (4°) para contactos con ángulos ASC/MC** en `calcularAspectosSinastria` (los ángulos son sensibles a errores de hora ~15 min ≈ 3-4°) y `_factorCasa7` (Descendente) reducido a 4°. **Contactos con el ASC alimentan sectores** (Sol/Venus/Marte-ASC → Química, Luna-ASC → Emocional, Mercurio-ASC → Mental) vía `PARES_FACTOR` con `angulo:true`. **La OPOSICIÓN al ASC = conjunción al Descendente (Casa 7) → Compromiso** (se salta en el loop para no duplicar; la gestiona `_factorCasa7`). **Aspectos que faltaban añadidos**: Venus-Urano y Marte-Neptuno (Química), Quirón-Venus/Sol/Luna (Emocional, `chiron:true`: la tensión SUMA como enganche kármico de sanación), Mercurio-Júpiter (Mental). Verificado: Diego×Paula **63%** (Pasión 82 — la oposición Marte-Neptuno 3°44' resta, correcto; Emocional 72 por Luna-ASC; Compromiso 50), Diego×Ainoa **61%** (Emocional 59). Sin errores JS. Version tags `?v=55`. **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Desglose: top 3 en barras, todos en detalles/copiar/prompt (2026-08-25)**: `_detalleFactor` ahora guarda **TODOS** los aspectos por sector (antes `slice(0,3)`). El desglose de barras de la app muestra **top 3** (`slice(0,3)` en `renderPanelFactores`), mientras que el **detalle técnico** (`renderDetallesTecnicos`), el **texto copiado** (`promptDataText`) y el **prompt de la IA** muestran **todos**. Verificado: Diego×Paula Química copia 6 aspectos (incl. Plutón△Marte 0°22' +18 que antes quedaba oculto), las barras muestran 3. Version tags `?v=56`. **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Prompt sinastría pulido (2026-08-25)**: `PROMPT_SINASTRIA` del worker sin marcadores de posición (evita duplicar tokens, ya que los datos reales van en el prompt de usuario), sin el término védico "Graha Maitri" (los datos son de astrología occidental/tropical), y con la micro-instrucción "Usa estrictamente los aspectos provistos; no asumas ni inventes aspectos adicionales". Rol aclarado a "astrología occidental". Cambio solo en `worker/oraculo-worker.js` (se despliega aparte en Cloudflare); no requiere rebuild del APK. **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **UI y fallback (2026-08-26)**: botones "Ver cartas guardadas" (astral) y "Ver sinastrías guardadas" (sinastría) ahora **siempre visibles** en sus formularios (como en Tarot). Al **recalcular otra sinastría se limpia el análisis anterior** (`#sinastria-interpretacion` + `window.__sinastriaUI._ultimoAnalisis`). El **fallback local** se reescribió para seguir la **misma estructura que el prompt IA** (=== ANÁLISIS INTEGRAL DE COMPATIBILIDAD === · 🌟 1 La Alquimia · 💫 2 Radiografía por Áreas (4 grupos) · 🔑 3 La Llave del Vínculo). El mensaje del fallback **distingue** "sin conexión a IA" de "límite de accesos alcanzado" (el worker devuelve 429 con "Límite/Cuota/Demasiadas"; la red lanza "Failed to fetch"). i18n: clave `ia.limiteIA` en 6 idiomas. Version tags `?v=60`. **Pendiente**: (1) redesplegar worker; (2) commit + push (esperando confirmación de Diego).
  - **Estado final sesión 5 (2026-08-26, `?v=62`)**: algoritmo definitivo con **8 factores** (Química 15%, Emocional 20%, Mental 15%, **Valores 15%**, Estabilidad 12%, Espiritual 10%, Compromiso 8%, Transformación 5%), **orbes dinámicos por cuerpo** (ángulos máx 4°, personal×personal estándar, personal×lento 5°, generacionales 1.5°, **quincuncio 150° solo personales 2.5°** con `mult=-0.7` en Valores/Emocional), contactos con el ASC que alimentan sectores, Quirón (tensión suma), Venus-Urano/Marte-Neptuno/Mercurio-Júpiter, media ponderada por orbe×planeta (escala 2.3), `_factorCasa7` con orbe Descendente 4°. **UI**: panel de 8 barras + radar octogonal con % central y vértices tappables, consejo "Punto a trabajar" bajo el diagrama, "Detalles técnicos" (8 sectores) + "Datos completos" (todos los aspectos, planetas con signo/casa, compuesta, casas, amistad), copiar = bloque del prompt IA, botón "Copiar sinastria + análisis", compartir incluye análisis, borrado con modal propio `confirmar()`, submenú de guardadas tipo toggle, badge IA centrado, botón Ko-fi restaurado. **Worker local** con `PROMPT_SINASTRIA` definitivo ("analista de relaciones", estructura `=== ANÁLISIS INTEGRAL ===` → 🌟💫🔑, 500-700 palabras, sin placeholders). `?v=62` uniforme en todos los js/index.html. **Worker redesplegado en Cloudflare (2026-08-26, confirmado por Diego)**: el desplegado ya usa el `PROMPT_SINASTRIA` definitivo "analista de relaciones"; el análisis IA de sinastría funciona con estructura `=== ANÁLISIS INTEGRAL ===` (🌟💫🔑). **Pendiente**: commit + push (esperando confirmación de Diego).
  - **Fallback local rework al estándar integral (2026-08-26, `?v=64`)**: el fallback sin IA ya NO genera HTML a mano; genera **texto plano markdown** idéntico en estructura al prompt IA y lo convierte con `markdownAHtml` (exportado desde `ia-api.js`). Encabezado único `=== ANÁLISIS INTEGRAL DE COMPATIBILIDAD ===`; cabecera limpia `"${globalScore}% — ${label}. <frase corta>"` (sin repetir la etiqueta 3 veces, helper `_fraseGlobalCorta`); 4 bloques 🌟 (alquimia + Sol/Luna compuesta) · 💫 (💗 Emocional+Valores, 🗨️ Mental+Estabilidad+Compromiso, 🔥 Pasión+Transformación, ✨ Espiritual) · 🔑 Llave del Vínculo, separados por `---`. **Selección de aspectos por sector** (`_seleccionarAspectosLocal`): Facilidad ≥70 → hasta 3 positivos por mayor +score y menor orbe; Desafío <50 → mayor fricción negativa o quincuncio de menor orbe; Intenso (Química/Transformación) → tensión que atrae Y flujo que une; Matiz → 2 positivos + 1 negativo. **Desempate en la Llave** (`_factorTop`): fortaleza = mayor score con prioridad Emocional>Química>Espiritual>Mental>Valores>Estabilidad>Compromiso; aprendizaje = menor score con prioridad Valores>Mental>Emocional>Estabilidad>Espiritual>Compromiso. El motor (`astrologia.js`) ahora exporta `factorAspectos` (aspectos estructurados por sector: planetas, signos, dueños, tipo, orbe, delta, fricción, símbolo) para alimentar la selección; `factorDetalle` se refactorizó sobre él. Verificado en emulador: DiegoVO×Ainoa → encabezado único, frase limpia "60% — Atracción con Matices", 3 headers, 6 listas, 2 divisores `<hr>`, selección correcta (Espiritual Facilidad → 3 positivos; Pasión/Transformación Intenso → flujo+tensión; fortaleza Espiritual 77, aprendizaje Valores 50 con desempate sobre Estabilidad/Compromiso). Sin errores JS. `?v=64` uniforme. **Pendiente**: commit + push (esperando confirmación de Diego).
  - **Bugfixes sinastría (2026-08-26, `?v=65`)**: (1) **`_regenteCasa7` roto**: los `regente` de `SIGNOS` están en español ('Marte', 'Luna'...) pero `carta.planetas` usa nombres ingleses ('Mars', 'Moon'...), así que el bonus del regente de casa 7 solo funcionaba para signos regidos por Venus. Añadido `REGENTE_EN` (español→inglés) y aplicado en `_regenteCasa7`. Verificado: DiegoVO×Ainoa Compromiso 50→46 (ahora detecta tensión entre regentes) y global 60→59%. (2) **Typo `afininadMental`** en `PESOS_PARES` (Mercury-Venus/Venus-Mercury) corregido a `afinidadMental`; eliminado el workaround `pesoPar.dim === 'afininadMental' ? 'afinidadMental' : ...` en `_dimensionDePar`. (3) **`_detalleCasa7` orbe 8° vs `_factorCasa7` 4°**: alineado a 4° para que el detalle no muestre planetas "junto al Descendente" que no puntúan. (4) **`index.html` raíz desincronizado**: le faltaba el contenedor `sinastria-factores` (el panel de 8 factores no se renderizaba si se abría ese index); sincronizado desde `js/index.html`. `?v=65` uniforme. **Pendiente**: commit + push (esperando confirmación de Diego).
  - **Restaurados botones copiar/compartir "todo" (2026-08-26, `?v=66`)**: el `index.html` raíz tenía botones de tarot/astral que la app (assets) había perdido. Restaurados en `js/index.html` (y sincronizados a raíz/www/assets): **Carta Astral** → `btn-copiar-astral-todo` ("Copiar todo con análisis") y `btn-compartir-astral-todo` ("Compartir todo"), ocultos por defecto y mostrados tras generar el análisis (los muestra `main.js`); `btn-compartir-astral` ("Compartir carta astral") siempre visible. **Tarot** → `btn-compartir` ("Compartir datos", se muestra tras la tirada) y `btn-compartir-todo` ("Compartir todo", tras el análisis). Los handlers los añade `astral.js`/`tarot.js` vía `addEventListener` (sin `onclick` inline para no duplicar). Verificado en emulador: tras tirada de Tarot aparece `btn-compartir`; tras análisis de Carta Astral aparecen `btn-copiar-astral-todo` y `btn-compartir-astral-todo` (display:block). `?v=66` uniforme. **Pendiente**: commit + push (esperando confirmación de Diego).
  - **Bugfixes análisis local (2026-08-26, `?v=68`)**: (1) **Placeholders rotos en el fallback de Carta Astral** (`astrologia-analisis.js`): los templates de `datos-maestros` usan `${sqSol}`, `${eiAsc}`, `${etDom}`, `${mtDom}`, `${masculine}`, `${feminine}`, `${solSLower}` pero el código solo reemplazaba `${SQ[solS]}`, `${EI[ascE]}`, `${ET[domEl]}`, `${MT[domMod]}`, `${st.masculine}`... → salían literales. Añadido un pase final de reemplazo en `analizarCartaAstral` (mapea `sqSol`→`SQ[solS]`, `eiAsc`→`EI[ascE]`, `etDom`→`ET[domEl]`, `mtDom`→`MT[domMod]`, `masculine`/`feminine`→`st.*`, `solSLower`→`solS.toLowerCase()`, etc.) y ampliado `_evalTpl` para los placeholders de aperturas/conectores de la sección 3 (`${area}`→`CT[n]`, `${signo}`→`s`, `${cualidad}`→`SQ[s]`, `${cualidadCorta}`→`SQ[s].split(',')[0]`, `${planeta}`→`p`). Verificado: el análisis local de Diego ya no muestra `${...}` (0 placeholders). (2) **Duplicación "No se especificó ninguna pregunta"** en el análisis de Tarot (`analysis.js`): cuando no hay pregunta, el aviso se añadía dos veces (línea 201 + `contextoPregunta`). Corregido: `contextoPregunta` queda vacío en el caso sin pregunta. Verificado: el aviso aparece 1 vez. (3) **Duplicación en encabezados de guardadas** (`main.js`): `renderCartasGuardadas` y `renderTiradasGuardadas` añadían "📚" y "(N)" dos veces (el i18n `main.cartasGuardadas`/`main.tiradasGuardadas` ya incluyen "📚" y "({n})"). Corregido: `'<h3>' + t(...) + '</h3>'`. Verificado: "📚 Cartas Astrales Guardadas (13)". `?v=68` uniforme. **Pendiente**: commit + push (esperando confirmación de Diego).
  - **Ronda 2 de revisión completa (2026-08-26, `?v=69`)**: revisadas todas las funcionalidades en emulador — tiradas de Tarot (1/3/Cruz Celta + I Ching), modales de carta y hexagrama, análisis combinado (IA), onboarding (6 pasos, i18n en 6 idiomas), modal Acerca de/Ko-fi, glosario (popover), cambio de idioma, guardar/ver/borrar guardadas (cartas, tiradas, sinastrías con modal de confirmación), radar octogonal y rueda bicarta de sinastría (tap en vértice/aspecto/planeta), secciones de Carta Astral (tabla, aspectos, stats), imágenes de cartas (webp). Todo funciona. **Bug corregido**: doble punto en los mensajes de guardadas vacías ("No hay tiradas guardadas.." → "No hay tiradas guardadas.") en `renderTiradasGuardadas`/`renderCartasGuardadas` (`main.js`). `?v=69` uniforme. **Pendiente**: commit + push (esperando confirmación de Diego).