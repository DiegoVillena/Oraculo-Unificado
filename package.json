{
  "name": "oraculounificado",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "npx cap sync android",
    "build:bundle": "npx cap sync android && cd android && ./gradlew bundleRelease",
    "build:apk:debug": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "sql.js": "^1.14.1"
  }
}
